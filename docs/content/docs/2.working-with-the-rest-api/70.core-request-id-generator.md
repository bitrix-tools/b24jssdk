---
title: RequestIdGenerator
description: 'Реализация `IRequestIdGenerator` по умолчанию. Генерирует request id в формате UUID v7 и предоставляет имена HTTP-заголовка / query-параметра, используемые SDK.'
category: 'Core'
navigation.title: RequestIdGenerator
links:
  - label: RequestIdGenerator
    iconName: GitHubIcon
    to: https://github.com/bitrix24/b24jssdk/blob/main/packages/jssdk/src/core/request-id-generator.ts
  - label: IRequestIdGenerator
    iconName: GitHubIcon
    to: https://github.com/bitrix24/b24jssdk/blob/main/packages/jssdk/src/types/http.ts
---

`RequestIdGenerator`{lang="ts-type"} — это реализация `IRequestIdGenerator`{lang="ts-type"} по умолчанию, используемая HTTP-клиентами для того, чтобы:

- генерировать уникальный id для каждого исходящего запроса через [`Text.getUuidRfc4122()`](/docs/working-with-the-rest-api/tools-text/) (UUID v7),
- определять, какой HTTP-заголовок его несёт (`X-Request-ID`),
- определять, какие query-параметры несут его и метаданные SDK (`bx24_request_id`, `bx24_sdk_ver`, `bx24_sdk_type`).

Переопределять это нужно только когда вы хотите серверную корреляцию с существующей схемой трассировки (например, пробросить request id из вышестоящего сервиса) — реализуйте `IRequestIdGenerator`{lang="ts-type"} и внедрите его через кастомную настройку `Http`.

## Константы по умолчанию {#default-constants}

| Константа | Значение |
|----|----|
| Поле заголовка | `X-Request-ID` |
| Query-параметр | `bx24_request_id` |
| Query-параметр (версия SDK) | `bx24_sdk_ver` |
| Query-параметр (тип SDK) | `bx24_sdk_type` |

Последние два постоянны в рамках сборки (заменяются на этапе компиляции через `unbuild`).

## API {#api}

```ts-type
class RequestIdGenerator implements IRequestIdGenerator {
  getRequestId(): string                       // UUID v7
  getHeaderFieldName(): string                 // 'X-Request-ID'
  getQueryStringParameterName(): string        // 'bx24_request_id'
  getQueryStringSdkParameterName(): string     // 'bx24_sdk_ver'
  getQueryStringSdkTypeParameterName(): string // 'bx24_sdk_type'
}
```

## Интерфейс `IRequestIdGenerator` {#irequestidgenerator-interface}

```ts-type
interface IRequestIdGenerator {
  getRequestId(): string
  getHeaderFieldName(): string
  getQueryStringParameterName(): string
  getQueryStringSdkParameterName(): string
}
```

Если вы предоставляете свою реализацию, убедитесь, что `getRequestId()` возвращает значение, **уникальное для каждого вызова** — лимитер и журнал запросов используют его как ключ.

---
title: Телеметрия
description: 'Query-параметры request id, версии SDK и типа SDK, добавляемые к каждому REST-вызову Bitrix24.'
category: 'Core'
navigation.title: Телеметрия
links:
  - label: RequestIdGenerator
    iconName: GitHubIcon
    to: https://github.com/bitrix24/b24jssdk/blob/main/packages/jssdk/src/core/request-id-generator.ts
---

SDK добавляет три телеметрических query-параметра к каждому исходящему REST-запросу. Их имена стабильны между версиями; их значения позволяют сопоставлять запросы SDK на стороне Bitrix24 и в вашей инфраструктуре (логи, трейсы, дашборды).

| Параметр | Источник | Примечания |
|----|----|----|
| `bx24_request_id` | `requestId`, который вы передаёте в `actions.v{2,3}.*.make({ requestId })`, или UUID v7, сгенерированный [`RequestIdGenerator`](/docs/working-with-the-rest-api/core-request-id-generator/), если опущен. | Отправляется как query-параметр и как заголовок `X-Request-ID`. Все три телеметрических параметра **пропускаются для legacy-позиционных методов `task.*`** (`task.commentitem.*`, `task.checklistitem.*`, …) в **обоих** REST API v2 и v3 — эти методы читают query-строку позиционно, поэтому Bitrix24 отклоняет вызов (`WRONG_ARGUMENTS`), если параметры присутствуют. Современный `tasks.task.*` этим ограничением не затронут. |
| `bx24_sdk_ver` | Константа времени сборки, заменяемая `unbuild`. Равна версии из `package.json` пакета. | Позволяет Bitrix24 различать проблемы по версии SDK. |
| `bx24_sdk_type` | Константа времени сборки. Сейчас `b24-js-sdk`. | Отличает `b24jssdk` от других SDK (PHP, Python). |

## Имя заголовка {#header-name}

То же значение `bx24_request_id` также помещается в заголовок `X-Request-ID` (`DEFAULT_REQUEST_ID_HEADER_FIELD_NAME` в [`RequestIdGenerator`](/docs/working-with-the-rest-api/core-request-id-generator/)). Если ваш edge-слой логирует `X-Request-ID`, вы увидите один и тот же id в своих логах и в логах сервера Bitrix24.

## Кастомизация генератора {#customising-the-generator}

Если нужно унаследовать вышестоящий id трассировки (например, пробросить `X-Request-ID` из вашего шлюза), реализуйте [`IRequestIdGenerator`](/docs/working-with-the-rest-api/core-request-id-generator/) и внедрите его через кастомную настройку HTTP. Каждый вызов должен возвращать уникальное значение — лимитер и журнал запросов используют его как ключ.

## Всегда передавайте `requestId` {#always-pass-requestid}

Хотя SDK генерирует его за вас, **всегда задавайте `requestId` явно** в production-коде:

```ts
await $b24.actions.v2.call.make({
  method: 'crm.contact.get',
  params: { id: 123 },
  requestId: `contact-${123}-${Date.now()}`
})
```

Причины:

- Id появляется в выводе логгера SDK, упрощая корреляцию.
- Id используется `RestrictionManager` для учёта повторных запросов / backoff.
- Осмысленный id гораздо легче найти grep'ом в тикетах поддержки Bitrix24, чем UUID.
